<launch>
  <node name="translation_estimator" pkg="translation_estimator" type="translation_estimator_node" output="screen">
  
    <!-- <param name="filename" type="string" value="$(find event_publisher)/config/config.yaml" /> -->
    <param name="yaml" type="string" value="$(find translation_estimator)/config/config.yaml" />
    <param name="reader_yaml" type="string" value="$(find event_publisher)/config/config.yaml" />

  </node>

</launch>
